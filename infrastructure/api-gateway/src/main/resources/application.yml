server:
  port: 8080

spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      server:
        webflux:
          routes:
            # 외부에서 내부 서비스용 API 호출 시 차단
            - id: block-external-internal-api
              uri: no-reply://
              predicates:
                - Path=/internal/**
              filters:
                - SetStatus=403

            # user-service - 비인증 허용
            - id: user-public
              uri: lb://user-service
              predicates:
                - Path=/api/v1/users/signup, /api/v1/users/login
              filters:
                - name: JwtAuthFilter
                  args:
                    requireAuth: false

            # user-service - 인증 필수
            - id: user-service
              uri: lb://user-service
              predicates:
                - Path=/api/v1/users/**
              filters:
                - name: JwtAuthFilter
                  args:
                    requireAuth: true

            # logistics-service - 인증 필수
            - id: logistics-service
              uri: lb://logistics-service
              predicates:
                - Path=/api/v1/hubs/**, /api/v1/hub-routes/**, /api/v1/delivery-agents/**
              filters:
                - name: JwtAuthFilter
                  args:
                    requireAuth: true
            # 4. 카탈로그 서비스 (질문자님 담당 - v1 반영) - 인증 필수 라우트
            - id: catalog-service
              uri: lb://catalog-service
              predicates:
                - Path=/api/v1/companies/**, /api/v1/products/**
              filters:
                - name: JwtAuthFilter
                  args:
                    requireAuth: true

            # order-service - 인증 필수
            - id: order-service
              uri: lb://order-service
              predicates:
                - Path=/api/v1/orders/**
              filters:
                - name: JwtAuthFilter
                  args:
                    requireAuth: true

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka/

jwt:
  secret: ${JWT_SECRET_KEY:dGVzdC1zZWNyZXQta2V5LWZvci1jaS1tdXN0LWJlLWxvbmctZW5vdWdo}